workflows:
  ios-workflow:
    name: iOS Build
    max_build_duration: 60
    instance_type: mac_mini_m2
    environment:
      flutter: stable
    scripts:
      - name: Flutter build
        script: |
          flutter pub get
          flutter build ipa --release
    artifacts:
      - build/ios/ipa/*.ipa
