workflows:
  ios-workflow:
    name: iOS Build
    max_build_duration: 60
    instance_type: macos_ventura
    environment:
      flutter: stable
    scripts:
      - name: Flutter build
        script: |
          flutter pub get
          flutter build ios --no-codesign
